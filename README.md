# Generating_MNIST_numbers_with_VAE_and_CVAE

This repository has an example model using Variational Autoencoder (VAE) and a Conditional Variational Autoencoder (CVAE) to generate numbers in MNIST images.

